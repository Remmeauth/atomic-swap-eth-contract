# Contract for Remme atomic swap

## Install:
require 
```
npm install truffle
npm install ganache
```
install dependencies

`npm install`

## Test

`truffle test`


## Compile

`truffle compile`

## Deploy

`truffle migrate`

may also need to specify network e.g.  `--network ganache`
